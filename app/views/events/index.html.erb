<h2>Events!</h2>

<% @events.each do |event| %>
  <div class="event-container">
    <h3><%= event.name %></h3>
    <p><%= event.location %></p>
    <p><%= event.start_time %></p>
    <p><%= event.end_time %></p>
    <%= link_to "Details", event %>
  </div>
<% end %>